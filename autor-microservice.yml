
# HTTP Server
server:
  port: 9401   # HTTP (Tomcat) port

# Eureka Server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

# Loggin Mongo Querys
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
